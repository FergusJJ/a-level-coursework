package zalando

import (
	utils "github.com/FergusJJ/coursework/gosrc/utils"
)

func InitTask(profile *utils.Profile) {
	return
}

func StartTask() {

	//send get request to zalando to get xsrf, then send to API and get cookie back, use to log in, then go to product link
	//add product to cart, checkout, get a final abck cookie then submit order
	//webhook sent after that and goroutine is complete

}
